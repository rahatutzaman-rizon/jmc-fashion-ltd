/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['sandbox.sslcommerz.com'], // Add your external image domain here
    },
  };
  
  export default nextConfig;
  